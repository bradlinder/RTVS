# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.spec
*.zip
*.tar
*.tar.gz
*.tgz
*.7z

# Virtual environments
venv/
.venv/
env/
ENV/

# Logs and databases
*.log
*.sqlite3

# OS files
.DS_Store
Thumbs.db

# Stray Node/Vite/AI-Studio scaffolding (unrelated to the PySide6 desktop
# app; see roadmap notes -- this should not be installed/committed at all
# once cleaned up, but ignore it in the meantime so it can't leak secrets
# or bloat future commits)
node_modules/
.vite/
.env.local
.env.*.local

# Environment variables
.env*
!.env.example

# AI Studio web preview scaffolding (keeps GitHub repo focused on Python app)
node_modules/
bun.lock
package.json
package-lock.json
tsconfig.json
vite.config.ts
metadata.json
index.html
src/
public/
coverage/

